import { ExampleConfig } from "../typings";

const headerConfig: ExampleConfig = {
  title: "Type",
  text: "Raw Text",
  configs: [{ text: "Dev" }, { text: "Dist" }, { text: "Npm" }],
};

export default headerConfig;
